# [task] Cross-category diverse-start EVAL task: same object pool / sim as
# single_lift_xcat_diverse, but LOW lifted-clear success band + brief hold so the
# harness early-stops on success (no carry-down). Mirrors
# single_lift_banana_soft_diverse_eval.
# Used by: experiments (task:) single_lift_xcat_diverse_eval
# Status: experimental
success_z_min: 0.13
success_z_max: 0.45
hold_steps: 4
object_name: "mushroom"
object_type: "soft"
sim_substeps: 420
mpm_grid_density: 170
success_scale: 0.2
rewards:
  stress:      {scale: 0.2, cap: 1.5, mean_weight: 0.2, top10_weight: 0.8}
  dist_to_obj: {scale: 0.1, decay: 8.0}
  lift:        {scale: 0.1, grasp_gate_dist: 0.079, lift_target: 0.16}
