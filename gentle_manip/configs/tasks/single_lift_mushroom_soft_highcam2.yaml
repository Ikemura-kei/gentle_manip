# [task] Camera-placement STUDY fork #2 of single_lift_mushroom_soft.yaml — ONLY cam_ext extrinsic.
# Used by: experiments (task:) single_lift_mushroom_soft_abs_action_highcam2 -> v3 demo collection (viz)
# Status: experimental (camera study: cam_ext raised +30cm from original = +15cm above the highcam fork)
#
# cam_ext raised +30cm in z from the calibrated L515 pose (0.09825304 -> 0.39825304) = 15cm higher
# than single_lift_mushroom_soft_highcam.yaml (+15cm), same lookat = object DR center (0.47, 0, 0).
# A yet-more top-down depth view.
cam_pos:    [0.98910661, -0.00034108, 0.39825304]
cam_lookat: [0.47, 0.0, 0.0]

# ── everything below is identical to single_lift_mushroom_soft.yaml ──
success_z_min: 0.175
success_z_max: 0.275
hold_steps: 30
object_name: "mushroom"
object_type: "soft"

sim_substeps: 220
mpm_grid_density: 250

success_scale: 0.2

rewards:
  stress:
    scale: 0.2
    cap: 1.5
    mean_weight: 0.2
    top10_weight: 0.8
  dist_to_obj:
    scale: 0.1
    decay: 8.0
  lift:
    scale: 0.1
    grasp_gate_dist: 0.079
    lift_target: 0.16
