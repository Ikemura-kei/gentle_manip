# [task] Camera-placement fork of single_lift_mushroom_soft.yaml — ONLY the cam_ext extrinsic changes.
# Used by: experiments (task:) single_lift_mushroom_soft_abs_action_highcam[_rot6d] -> v3 demo collection
# Status: experimental (higher, more top-down depth view — better object size/shape in the point cloud)
#
# cam_ext raised +30cm in z from the calibrated L515 pose (0.09825304 -> 0.39825304) and re-aimed at the
# object-pose randomization CENTER (0.47, 0, 0) at z=0 -> a top-down depth profile.
cam_pos:    [0.98910661, -0.00034108, 0.39825304]
cam_lookat: [0.47, 0.0, 0.0]

# ── everything below is identical to single_lift_mushroom_soft.yaml ──
success_z_min: 0.175
success_z_max: 0.275
hold_steps: 30
object_name: "mushroom"
object_type: "soft"

sim_substeps: 235
mpm_grid_density: 250

success_scale: 0.2

rewards:
  stress:
    scale: 0.2
    cap: 1.5
    mean_weight: 0.2
    top10_weight: 0.8
  dist_to_obj:
    scale: 0.1
    decay: 8.0
  lift:
    scale: 0.1
    grasp_gate_dist: 0.079
    lift_target: 0.16
