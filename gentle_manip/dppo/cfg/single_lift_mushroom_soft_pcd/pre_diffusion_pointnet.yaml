# [dppo-pretrain] BC-pretrain a PointNet diffusion policy on SOFT mushroom lift demos.
# Used by: third_party/dppo/script/run.py (TrainDiffusionAgent) via gentle_manip.dppo.train
# Status: experimental (DPPO step-2: point-cloud student pipeline, the real target)
#
# PointNet-conditioned DiffusionMLP on the deployable student view: 8-dim proprio
# (ee_pos+quat+gripper) + a 1024-pt cloud (PointNetDiffusionMLP encodes the cloud, DP3-style).
# Data = gentle_manip.dppo.convert_demos --point-cloud (states + point_cloud in train.npz).
# Run with envs/dppo.
defaults:
  - _self_
hydra:
  run:
    dir: ${logdir}
  callbacks:                    # snapshot the env (experiment) config -> <run>/config/
    experiment_snapshot:
      _target_: gentle_manip.dppo.hydra_snapshot.ExperimentSnapshot
_target_: agent.pretrain.train_diffusion_agent.TrainDiffusionAgent

name: ${env}_pre_diffusion_pointnet_ta${horizon_steps}_td${denoising_steps}
# logdir leaf = the global 5-letter experiment ID (exp_id resolver; registered in experiments.csv)
logdir: ${oc.env:DPPO_LOG_DIR}/dppo-pretrain/${env}/${exp_id:}
train_dataset_path: ${oc.env:DPPO_DATA_DIR}/${env}/train.npz
experiment: single_lift_mushroom_soft   # env definition (task+obs+action+dr+reward) snapshot

seed: 42
device: cuda:0
env: single_lift_mushroom_soft_pcd
obs_dim: 8                    # proprio only (ee_pos3 + ee_quat4 + gripper1); cloud is separate
action_dim: 7
denoising_steps: 20
horizon_steps: 4
cond_steps: 2                 # proprio history
pc_cond_steps: 1             # clouds encoded per step (<= cond_steps)
n_points: 1024
visual_feature_dim: 256

wandb:                          # enabled; disable per-run with `wandb=null`, or WANDB_MODE=offline
  entity: ${oc.env:DPPO_WANDB_ENTITY,null}   # null -> logged-in default account
  project: gentle-manip-${env}
  run: ${exp_id:}               # wandb run name == the experiment ID (matches the log dir)
  group: dppo-pretrain

train:
  n_epochs: 3000
  batch_size: 128
  learning_rate: 1e-4
  weight_decay: 1e-6
  lr_scheduler:
    first_cycle_steps: 3000
    warmup_steps: 1
    min_lr: 1e-5
  epoch_start_ema: 10
  update_ema_freq: 5
  save_model_freq: 500

model:
  _target_: model.diffusion.diffusion.DiffusionModel
  predict_epsilon: True
  denoised_clip_value: 1.0
  network:
    _target_: gentle_manip.dppo.pointnet_diffusion.PointNetDiffusionMLP
    action_dim: ${action_dim}
    horizon_steps: ${horizon_steps}
    cond_dim: ${eval:'${obs_dim} * ${cond_steps}'}
    pc_cond_steps: ${pc_cond_steps}
    visual_feature_dim: ${visual_feature_dim}
    time_dim: 16
    mlp_dims: [512, 512, 512]
    activation_type: ReLU
    residual_style: True
    pointnet:
      in_channels: 3
      use_layernorm: True
      final_norm: layernorm
  horizon_steps: ${horizon_steps}
  obs_dim: ${obs_dim}
  action_dim: ${action_dim}
  denoising_steps: ${denoising_steps}
  device: ${device}

ema:
  decay: 0.995

train_dataset:
  _target_: gentle_manip.dppo.pointcloud_dataset.StitchedSequencePointCloudDataset
  dataset_path: ${train_dataset_path}
  horizon_steps: ${horizon_steps}
  cond_steps: ${cond_steps}
  pc_cond_steps: ${pc_cond_steps}
  device: ${device}
