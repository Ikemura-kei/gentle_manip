# Vendored binary tool (not an asset; the sim uses the .urdf only)
gentle_manip/assets/xarm/lc_soft_enable_wide_grip/urdf-viz

# Generated figures/videos from example scripts (point-cloud viz, calibration sweeps,
# soft-body dev renders)
examples/*.png
examples/*.mp4

# sim2real diagnosis artifacts — exploratory OUTPUTS (figures/videos), new + future, kept
# LOCAL. One-off diagnostic scripts also stay local (not committed); reusable fixes that land
# in shared code are committed as usual.
examples/sim2real_diagnose/figures/
examples/sim2real_diagnose/scripted_in_sim_at_real_positions.py

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
*.egg

# Virtual envs
.venv/
venv/
env/

# Pytest
.pytest_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Collected demo datasets + real-deploy recordings (large point-cloud pickles +
# summary PNGs/MP4s). Keep small calibration artifacts (dataset/camera_calibration/) tracked.
dataset/demos/
dataset/real_deploy/
dataset/serl_clips/
# DPPO pretrain npz — derived from dataset/demos via gentle_manip.dppo.convert_demos
dataset/dppo/
logs/
# Scratch/smoke-test collection dirs + unrelated large local data (not campaign deliverables)
dataset/demos_smoketest*/
dataset/demos_merged*_TEMP/
dataset/demos_retry/
dataset/demos_retry_v2/
dataset/demos_retry_earlyabort_test/
dataset/demos_retry_video_samples/
dataset/tactile_dp3/
# DPPO-converted npz for the retry-specialist campaign (dppo_data_retry/) — same
# derived-data role as dataset/dppo/ above, just a separate dir for this campaign
dppo_data_retry/
# DPPO-converted npz for the OmniReset-inspired diverse-start campaign (2026-08-28)
# — same derived-data role as dataset/dppo/ above. NOT in git; transfer separately
# (rsync/scp) when migrating to another machine/cluster. See
# docs/HANDOFF_cluster_migration_2026-08-28.md.
dppo_data_diverse/
# Legacy standalone-DP3 zarr conversion (experimental, not the active training path
# -- see docs/dppo_dp3_training_recipe.md §0). Large binary, not in git.
dataset/dp3/
# tetgen tet-mesher scratch output (written to cwd during FEM build_grasp_fem)
_skipped.face
_skipped.node

# Experiment registry — local index of local training runs (absolute run_dir paths, changes
# every run). Rebuildable from the log tree via scripts/reconcile_experiments.py.
experiments.csv

# wandb local run data (synced to the cloud; the run dir is the source of truth)
wandb/

# Point-cloud focus comparison videos (viz output of examples/render_pntcld_focus.py)
examples/pntcld_focus/

# experiment_registry transient sidecars (flock + atomic-write temp)
experiments.csv.lock
experiments.csv.tmp

# MPM grid/substep sweep video outputs (examples/mushroom_grid_substep_sweep.py --video)
examples/mushroom_sweep/

# Isaac deformable capture dumps (play_deformables.py --dump; large npz + rendered videos)
gentle_manip/isaac/captures/
gentle_manip/isaac/viz_out/
# Isaac converted assets (URDF->USD output; large, regenerable via the README recipe)
gentle_manip/isaac/assets/

# smgrasp FEM stress-viz outputs (PNG/MP4/VTU renders — regenerable via the viz_* demos)
grasp_synthesis/viz_out/

# Scratch / writing files kept out of the repo
tmp.csv
paper_writing.txt

# Report-generation scratch (persistent alternative to /tmp, which gets cleared
# periodically on this box and has twice wiped the in-progress GRACE report
# build script + rendered videos mid-campaign, 2026-08-19)
.report_scratch/

# third_party/reactive_diffusion_policy (~18GB, checked 2026-08-28) is a plain local
# checkout, NOT a git submodule (not in .gitmodules) -- safety net so it can never be
# swept into a commit by a broad `git add`.
third_party/reactive_diffusion_policy/
