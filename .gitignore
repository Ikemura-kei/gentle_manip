# Vendored binary tool (not an asset; the sim uses the .urdf only)
gentle_manip/assets/xarm/lc_soft_enable_wide_grip/urdf-viz

# Generated figures/videos from example scripts (point-cloud viz, calibration sweeps,
# soft-body dev renders)
examples/*.png
examples/*.mp4

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
*.egg

# Virtual envs
.venv/
venv/
env/

# Pytest
.pytest_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Collected demo datasets + real-deploy recordings (large point-cloud pickles +
# summary PNGs/MP4s). Keep small calibration artifacts (dataset/camera_calibration/) tracked.
dataset/demos/
dataset/real_deploy/

# Tactile-DP3 baseline: converted zarr datasets + training checkpoints (large,
# machine-specific — regenerate via convert_tactile_demo_to_zarr.py / train_tactile_dp3.py).
dataset/tactile_dp3/
wandb/

# RDP baseline (third_party/reactive_diffusion_policy): downloaded datasets,
# training outputs/checkpoints, and wandb logs are large and machine-specific.
third_party/reactive_diffusion_policy/data/
third_party/reactive_diffusion_policy/wandb/
